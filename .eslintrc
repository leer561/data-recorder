{
    "extends": "eslint-config-egg",
    "rules": {
        //禁止在语句末尾使用分号 (除了消除以 [、(、/、+ 或 - 开始的语句的歧义)
        "semi": [
            1,
            "never"
        ],
        //允许箭头函数参数不使用（）
        "arrow-parens":0,
         "indent": [0]
    }
}
